/target
**/*.rs.bk
*.pdb

# Local build outputs (Windows cross-compiles, etc.)
/build

# Editor / OS cruft
.DS_Store
*.swp
.idea/
.vscode/
openpaint-document.openpaint*

# Test exports. `Ctrl+S` writes next to the working directory, so a few of these got
# committed by accident -- they are 350 KB each of nothing.
/openpaint-*.png
